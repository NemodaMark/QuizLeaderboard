@page "/dashboard"
@inject NavigationManager Nav

<h3>Dashboard</h3>

<p>Welcome to the dashboard. (Később ide jöhetnek a statok, leaderboard, stb.)</p>

<button class="btn btn-primary" @onclick="@(() => Nav.NavigateTo("/"))">
    Back to home
</button>